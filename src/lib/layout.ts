/** Horizontal page gutters — apply on the full-width shell (section, header, hero band). */
export const contentGutterClass = "px-4 sm:px-6 lg:px-8"

/** Centered content column; pair with `contentGutterClass` on the parent, not here. */
export const contentContainerClass = "mx-auto w-full max-w-6xl"
